{
  "name": "fonteia",
  "private": true,
  "version": "0.1.0",
  "description": "Fonte.ia by Olli - public-data intelligence platform for Brazil.",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "turbo dev",
    "build": "node scripts/run-workspaces.mjs build",
    "test": "node scripts/run-workspaces.mjs test",
    "lint": "node scripts/run-workspaces.mjs lint",
    "typecheck": "node scripts/run-workspaces.mjs typecheck",
    "deploy:cloudflare:web": "wrangler pages deploy apps/web/dist --project-name=fonteia --branch=main",
    "cloudflare:whoami": "wrangler whoami",
    "docs:sources": "tsx scripts/generate-source-docs.mjs",
    "turbo:build": "turbo build",
    "turbo:test": "turbo test",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "prettier": "^3.4.2",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8",
    "wrangler": "4.99.0"
  }
}
