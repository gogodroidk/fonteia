# Caça a bugs — 2026-06-20 (revisão multi-agente)

5 agentes varreram escopos disjuntos do código **em produção**; um verificador cético
refutou ~18 falsos-positivos. Aqui só o que **sobrou confirmado**, com status.
Máquina: `pnpm lint` 0 erros · `pnpm typecheck` 0 erros · `pnpm test` verde · build web verde.

## ✅ Corrigido nesta sessão (commitado)

| Arquivo | Bug | Correção |
|---|---|---|
| `packages/sources/.../receita-leiloes.ts:87` | `raw.valor` ausente → `minimumBidCents = NaN` propaga pro score e display R$ | guarda `Number.isFinite` |
| `packages/scoring/src/leiloes.ts:177` | `maxSuggestedBidCents = NaN` quando bid é NaN | guarda `Number.isFinite` |
| `packages/ai/src/answer-engine.ts:93` | `keyFact` sem citação resolvível ainda saía como "answered" (viola rastreabilidade) | filtra claims sem evidência citável; vira `insufficient_evidence` |
| `packages/sources/.../camara-proposicoes.ts:202` | `numero/ano` ausentes → título "PL NaN/NaN" | guarda `Number.isFinite` |
| `apps/web/src/lib/use-plan.ts` | plano não re-busca em login/logout/cupom (congela até reload); erro do `my_plan` rebaixa pra free em silêncio | re-fetch em `onAuthStateChange` + log do erro |
| `apps/web/src/components/admin/use-is-admin.ts` | sem `.catch` → nav admin trava em `loading` pra sempre numa falha de rede | adiciona `.catch` |
| `apps/web/src/lib/consent.ts` | `JSON.parse` sem validar shape → consentimento LGPD corrompido passa | valida campos boolean |
| `packages/domain/src/domain.test.ts:19` | teste **stale**: afirmava todos os módulos exceto leilões "locked", mas 8 estão "active" → falharia em `pnpm test` | atualizado pra realidade |
| `infra/migrations/0003_*.sql` | `REVOKE` antes do `CREATE` (0099) → recriação do zero aborta | `DO`-block tolerante |

## 📋 Aberto — alto valor (precisa decisão ou redeploy)

**Billing / banco**
- `my_plan()` (0099) inclui `past_due` no conjunto "ativo" → mantém acesso durante dunning. *Decisão de produto:* é o padrão (período de tolerância). Confirmar se é intencional.
- Webhook correlaciona assinatura **só por e-mail**; `subscription.created/updated` nunca grava e-mail e `invoice.customer_email` costuma vir null → liberação frágil. *Fix:* correlacionar por `stripe_customer_id` / backfill de e-mail. (redeploy do webhook)
- RLS `0002`: `email = auth.email()` (case-sensitive) vs `my_plan()` `lower()` → select direto na tabela diverge por caixa. *Fix:* `lower()` na policy.
- `0015/0016`: funções ingest sem `REVOKE` no próprio arquivo (prod **já protegida** via `0017`) → backfill nos arquivos pra DR.
- `0011` índice único do CKAN não escopado por `kind` → colisão entre kinds.

**Edge Functions (precisa redeploy)**
- **P0** `ingest-orgaos` pagina por Range **sem `order`** → pula/duplica linhas >1000. *Fix:* `&order=id.asc`.
- **P0** `ingest-inpe-queimadas` no resume trata 1º chunk de dados como header → focos corrompidos.
- **P1** `infosimples-proxy` trava de gasto é check-then-act → concorrência pode estourar o teto pago. (proxy dormente)
- **P1** `ingest-camara-despesas` `?max=0` ("todas") vira 10 + trunca silenciosamente.
- **P1** `ingest-pncp-contratos` grava CNPJ cru em `attributes.fornecedorCnpj` (coluna `entities.cnpj` ok).
- **P2** `fonteia` `/ai/intent` não capa `context` (custo); `ingest-brasilapi` 429→404 some; `ingest-sp-capital` `total=0` para cedo; `ingest-juridico` sem try/catch aborta todos os anos.

**Frontend**
- **P0/P1** `App.tsx` deep-link: abrir/atualizar `/app/municipios|politica|ambiental/:id` direto mostra "não encontrado" (sem fetch-by-id como o lot-detail). *Fix:* loaders por rota.
- **P1** `cerebro/page.tsx` ignora `?cnpj` ao montar → canvas vazio após navegar (liga direto à reclamação do Cérebro).
- **P1** `dossie/page.tsx` Enter dispara submit 2× (onKeyDown + form onSubmit).
- **P1** `lot-detail-page.tsx` timers de sucesso acumulam sem `clearTimeout`.
- **P1** `lib/api-client.ts` usa publishable key como Bearer (quebra rotas `verify_jwt=true`); `stripe-portal-client` usa `getSession` não validado no servidor; `account` `formatUntil` sem ano.
- **P2** `data/leiloes.ts`: `daysUntil` retorna 30 pra data inválida (risco "baixo" falso); `getEligibility` afirma "PJ" quando desconhecido; `estimatedValue` inventado (com prefixo "estim."). *(adiado: muda union de tipo)*
- **P2** `lotes` `imgLoaded` não reseta; `search` normalize de acento estreito; `cnpj.ts` sem dígito verificador.

## ❌ Falsos-positivos refutados (NÃO são bugs)
Verificados e descartados: "cancelled flag" stale em politica/municipios/ambiental; IntersectionObserver stale closure; `ai-client parsed.message` crash; `catalog-api` sem `.ok`; `alerts-api` NPE; `cerebro-api entry.status`; `leads-heuristics` case faltando; `external_ids` null em politica-api; double-encoding no link de busca. (~18 no total.)
