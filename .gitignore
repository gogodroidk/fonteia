node_modules/
.turbo/
.next/
dist/
build/
coverage/
.env
.env.*
!.env.example
*.log
*.tsbuildinfo
.expo/
.expo-shared/
android/
ios/


# Claude Code local state (worktrees + symlinks de skills)
.claude/

# Wrangler local dev cache/temp (gerado por `wrangler dev`)
.wrangler/

# Artefatos de trabalho do loop de QA (auditoria + prints) — nao versionar
.audit/
screenshots/
apps/web/shots.mjs

# D1 migration checkpoint files (generated by scripts/migrate-entities-to-d1.mjs)
scripts/.migrate-d1-checkpoint*.json
